import React from 'react';
import CommentListEntry from './CommentListEntry';

const CommentList = () => {
  return (
    <div>
      <CommentListEntry />
    </div>
  );
};

export default CommentList;
